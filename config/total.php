<?php

/**
 * Configuration for Total object for calculating back end totals
 */

/**
 * Set to numbers of decimal places to round to, will normally be 2
 */
$config['number_format'] = 2;

/**
 * Set to numbers of decimal places to round to, will normally be 2, if number formatting is not needed
 */
$config['round'] = FALSE;

/**
 * Before each number will attach a prefix after the suffix
 */
$config['prefix'] = NULL;

$config['suffix'] = NULL;
